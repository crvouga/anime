{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider && npm run develop",
    "explore": "gridsome explore",
    "sanity:start": "cd sanity-studio && npx @sanity/cli start",
    "sanity:deploy": "cd sanity-studio && npx @sanity/cli deploy",
    "preview": "docker build -t anime-blog . && docker run -p 80:80 anime-blog"
  },
  "dependencies": {
    "@gridsome/source-graphql": "^0.1.0",
    "@sanity/client": "^3.4.1",
    "bootstrap": "4",
    "bootstrap-vue": "^2.23.1",
    "graphql-request": "^5.0.0",
    "gridsome": "^0.7.0",
    "groq": "^2.33.2",
    "prop-types": "^15.8.1",
    "zod": "^3.19.1"
  }
}
